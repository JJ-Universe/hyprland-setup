{
    "layer": "top",
    "position": "bottom",
    "height": 53,
    "spacing": 6,

    "modules-left": [
        "custom/logo",
        "hyprland/workspaces",
        "hyprland/submap"
    ],
    "modules-center": [
        "hyprland/window",
        "clock"
    ],
    "modules-right": [
        "network",
        "pulseaudio",
        "custom/media",
        "cpu",
        "memory",
        "backlight",
        "tray",
        "custom/hint",
        "custom/power"
    ],

    "custom/logo": {
        "format": "󰈸",
        "tooltip": false,
        "on-click": "$HOME/.config/waybar/hint_menu.sh",
        "on-click-right": "pkill -SIGUSR2 waybar"
    },

    "hyprland/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{name}",
        "format-icons": {
            "urgent": "",
            "active": "",
            "default": ""
        }
    },

    "hyprland/submap": {
        "format": "<span style=\"italic\">{name}</span>",
        "tooltip": false
    },

    "hyprland/window": {
        "format": "{class} — {title}",
        "max-length": 68,
        "separate-outputs": true,
        "rewrite": {
            "^(.*) — Mozilla Firefox$": "$1 󰈹",
            "^(.*) — foot$": "$1 󰆍",
            "^(.*) — kitty$": "$1 󰄛"
        }
    },

    "tray": {
        "icon-size": 21,
        "spacing": 10
    },

    "clock": {
        "format": "  {:%a %d %b  %H:%M}",
        "format-alt": "  {:%Y-%m-%d}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "interval": 10
    },

    "network": {
        "interval": 5,
        "tooltip": false,
        "format-wifi": "  {signalStrength}%",
        "format-ethernet": "  {ifname}",
        "format-disconnected": " offline"
    },

    "cpu": {
        "format": "{usage}% ",
        "interval": 3,
        "tooltip": false
    },

    "memory": {
        "format": "{percentage}% ",
        "interval": 5,
        "tooltip": false
    },

    "backlight": {
        "format": "{percent}% {icon}",
        "interval": 2,
        "format-icons": ["", "", "", "", "", "", "", "", ""]
    },

    "pulseaudio": {
        "format": "{volume}% {icon} {format_source}",
        "format-bluetooth": "{volume}% {icon} {format_source}",
        "format-bluetooth-muted": " {icon} {format_source}",
        "format-muted": " {format_source}",
        "format-source": "{volume}% ",
        "format-source-muted": "",
        "format-icons": {
            "headphone": "",
            "hands-free": "",
            "headset": "",
            "phone": "",
            "portable": "",
            "car": "",
            "default": ["", "", ""]
        },
        "on-click": "pavucontrol"
    },

    "custom/media": {
        "format": "{icon} {text}",
        "return-type": "json",
        "max-length": 48,
        "format-icons": {
            "spotify": "",
            "default": "🎜"
        },
        "escape": true,
        "exec": "$HOME/.config/waybar/mediaplayer 2> /dev/null",
        "interval": 3,
        "on-click": "$HOME/.config/waybar/media_menu.sh",
        "on-click-right": "playerctl next",
        "on-click-middle": "playerctl previous",
        "on-scroll-up": "playerctl volume 0.03+",
        "on-scroll-down": "playerctl volume 0.03-"
    },

    "custom/hint": {
        "format": "",
        "tooltip": false,
        "on-click": "$HOME/.config/waybar/hint_menu.sh",
        "on-click-right": "pkill -SIGUSR2 waybar",
        "on-scroll-up": "playerctl volume 0.03+",
        "on-scroll-down": "playerctl volume 0.03-"
    },

    "custom/power": {
        "format": "⏻ ",
        "tooltip": false,
        "on-click": "if command -v wlogout >/dev/null 2>&1; then wlogout; elif command -v wofi >/dev/null 2>&1; then choice=$(printf 'Shutdown\nReboot\nSuspend\nHibernate\nLogout\n' | wofi --dmenu --prompt 'Power'); case \"$choice\" in Shutdown) systemctl poweroff;; Reboot) systemctl reboot;; Suspend) systemctl suspend;; Hibernate) systemctl hibernate;; Logout) hyprctl dispatch exit;; esac; elif command -v rofi >/dev/null 2>&1; then choice=$(printf 'Shutdown\nReboot\nSuspend\nHibernate\nLogout\n' | rofi -dmenu -p 'Power'); case \"$choice\" in Shutdown) systemctl poweroff;; Reboot) systemctl reboot;; Suspend) systemctl suspend;; Hibernate) systemctl hibernate;; Logout) hyprctl dispatch exit;; esac; else systemctl poweroff; fi"
    }
}


